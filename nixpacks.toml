[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt --break-system-packages"]

[start]
cmd = "python -m flask db upgrade && gunicorn 'app:create_app()' --bind 0.0.0.0:$PORT --workers 2 --timeout 30"
